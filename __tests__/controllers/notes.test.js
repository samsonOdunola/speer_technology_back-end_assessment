const NoteModel = require('../../models/notes');
const {createNote,
  getNote,
  updateNote,
  deleteNote,
  shareNote,
  getNoteById,
  search} = require('../controllers/notes.test');

describe('Create Notes Test', () => {

});
